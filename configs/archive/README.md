# Archived configurations

These files preserve earlier exploratory experiment settings. They are not
imported by `configs` and cannot be selected through `--config`.

Active configurations live directly under `configs/` and use dataset-neutral
registry names.
